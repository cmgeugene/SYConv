# Code Overview
- `exportToExcel()`: Simple utility function using `xlsx` to parse the cleaned JSON data into a worksheet and trigger a browser download.
- **Relationships & Flow**: Triggered by the "Export List" button in `App.jsx`.

# TODOs in this Code
- [ ] Map internal `parsedData` keys to human-readable Excel column headers.
