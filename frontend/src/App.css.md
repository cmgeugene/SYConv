# Code Overview
- `App.css`: The primary stylesheet for the application. Designed with modern vanilla CSS featuring vibrant but professional colors, glassmorphism, and smooth transitions.
- **Relationships & Flow**: Imported by `App.jsx` and defines the global styles and layout classes (e.g., `.app-container`, `.split-pane`, `.left-panel`, `.right-panel`).

# TODOs in this Code
- [ ] Define the split-pane CSS variables and layout flexbox.
- [ ] Style the file upload drag-and-drop zone.
- [ ] Add loading animations and micro-interactions.
